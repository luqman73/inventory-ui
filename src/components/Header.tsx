import LogoutButton from "./LogoutButton";

const Header = () => {
    return (
      <header>
        <nav>
          {/* navigation items */}
          <LogoutButton />
        </nav>
      </header>
    );
  };
  
  export default Header;